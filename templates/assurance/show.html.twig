{% extends 'base.html.twig' %}

{% block title %}Assurance{% endblock %}

{% block body %}
    <h1>Assurance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdA</th>
                <td>{{ assurance.idA }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ assurance.nom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ assurance.type }}</td>
            </tr>
            <tr>
                <th>DateExpiration</th>
                <td>{{ assurance.dateExpiration ? assurance.dateExpiration|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assurance_index') }}">back to list</a>

    <a href="{{ path('app_assurance_edit', {'idA': assurance.idA}) }}">edit</a>

    {{ include('assurance/_delete_form.html.twig') }}
{% endblock %}
