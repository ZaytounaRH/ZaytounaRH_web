{% extends 'base.html.twig' %}

{% block title %}Certification{% endblock %}

{% block body %}
    <h1>Certification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCertif</th>
                <td>{{ certification.idCertif }}</td>
            </tr>
            <tr>
                <th>TitreCertif</th>
                <td>{{ certification.titreCertif }}</td>
            </tr>
            <tr>
                <th>OrganismeCertif</th>
                <td>{{ certification.organismeCertif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_certification_index') }}">back to list</a>

    <a href="{{ path('app_certification_edit', {'idCertif': certification.idCertif}) }}">edit</a>

    {{ include('certification/_delete_form.html.twig') }}
{% endblock %}
